# لجر جاری بازنگری «شاهِد خیس»

این فایل فقط وضعیت اکنون را نگه می‌دارد؛ Git تاریخچهٔ ایشوهای بسته است.

## خط پایه

- دست‌نوشته: ۲۷ فایل در `drafts/chapters/`
- خط پایهٔ جاری: نسخهٔ پس از `WW-059` روی `main`
- وضعیت: ثبت اداری، اتصال حسی و مراقبت موضعی با واژگان ثابت و کنش‌های جدا روی قربانی قدیمی، پیش از ورود رها و هنگام جداسازی اعتبارسنجی شدند.
- شاخهٔ اجرا: `main`
- شمارهٔ بعدی ایشو: `WW-060`

## تصمیم‌های باز

ندارد.

## ایشوی فعال

ندارد.

## نتیجهٔ چرخهٔ جاری

`WW-059` قبول شد: آکسیوم ثبت و پذیرش اتصال تازه را می‌بندد، اژیدهاک کابل متصل را اجرا و جدا می‌کند و سِف فقط پس از پایان ثبت با کابل مانده به‌عنوان خطر بدنی عمل می‌کند.
