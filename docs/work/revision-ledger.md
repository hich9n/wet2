# لجر جاری بازنگری «شاهِد خیس»

این فایل فقط وضعیت اکنون را نگه می‌دارد؛ Git تاریخچهٔ ایشوهای بسته است.

## خط پایه

- دست‌نوشته: ۲۶ فایل در `drafts/chapters/`
- خط پایهٔ گذر لحن: `8163f4f`
- وضعیت: اجرای برنامهٔ تأییدشدهٔ گذر لحن روی نسخهٔ زنده
- برنامهٔ فعال: `docs/work/tone-pass-plan.md`
- شاخهٔ اجرا: `main`
- شمارهٔ بعدی ایشو: `WW-039`

## تصمیم‌های باز

ندارد.

## ایشوی فعال

### [~] WW-038 — اجرای گذر لحن روی دست‌نوشته

- نوع: نثر | دیالوگ
- مشکل: نقد بتاریدر راوی را در بخش‌هایی سنگین، ماشینی و ترجمه‌زده و رجیستر بعضی دیالوگ‌های انسانی را مختلط یافته است.
- اثر: فاصلهٔ عاطفی خواننده با کانون، ریتم استاکاتوی یکنواخت و لغزش میان گفتار کتابی و محاوره‌ای.
- شواهد:
  - `docs/work/tone-pass-plan.md` — تشخیص سه‌بخشی و نمونه‌های مصوبِ ترجمه‌زدایی و رجیستر.
  - `drafts/chapters/00*` تا `drafts/chapters/03*` — خط پایهٔ تأییدشدهٔ لحن هدف.
- قرارداد تغییر: اجرای دمای نثر، ترجمه‌زدایی و تفکیک رجیستر انسان/ماشین مطابق برنامه، واحدبه‌واحد.
- هدف‌ها: `drafts/chapters/00*` تا `drafts/chapters/25*`
- وابستگی: ندارد
- معیار پذیرش:
  - [ ] واحدهای ۰۰ تا ۰۳ فقط در سه دامنهٔ مجازِ پولیش بررسی شوند و تغییر حجم هرکدام حدود ۲٪ یا کمتر بماند.
  - [ ] در حرکت اول، نزدیکی و گرمای کانون؛ پس از انتقال، سردشدن تدریجی؛ و در اوج، ریتم تابع ظرفیت بدن باشد.
  - [ ] دیالوگ انسان‌ها محاوره‌ای و گفتار ماشین‌ها رسمی و متمایز بماند.
  - [ ] گرته‌برداری، سکتهٔ ناخواسته، کلاهک معنایی و واژه‌های ممنوع باقی نماند؛ مجموع «بی‌آن‌که/پیش از آن‌که» در هر واحد حداکثر دو باشد.
  - [ ] هر واحد چک عبارت، رجیستر، واژهٔ ممنوع و `git diff --check` را بگذراند.
- لازم است حفظ شود: همهٔ رخدادها، اشیا، اطلاعات دیالوگ، علت‌ها، ترتیب، کانون دید، بودجهٔ موتیف‌ها، قفل‌های سخت و ضربه‌های دست‌نزدنیِ برنامه.
- مجوز نویسنده: اجرای مستقیم طرح مصوب روی `main`، با یک ایشوی چتری و کامیت مستقل هر واحد؛ بدون push.

## نتیجهٔ چرخهٔ جاری

| واحد | خط پایه | جدید | تغییر | تصمیم اصلی | چک |
|---:|---:|---:|---:|---|---|
